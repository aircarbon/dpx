[profile.default]
src = "src"
out = "out"
libs = ["lib"]
# solc_version = "0.8.30"  # Commented out to allow auto-detection of multiple versions
optimizer = true
optimizer_runs = 200
auto_detect_solc = true
offline = false
remappings = [
    "@openzeppelin/contracts/=lib/openzeppelin-contracts/contracts/",
    "@openzeppelin/contracts-upgradeable/=lib/openzeppelin-contracts-upgradeable/contracts/",
    "@airswap/swap-erc20/=lib/airswap-protocols/source/swap-erc20/contracts/",
    "solady/=lib/solady/"
]

# RPC Endpoints - These reference .env variables
[rpc_endpoints]
mainnet = "${MAINNET_RPC_URL}"
sepolia = "${SEPOLIA_RPC_URL}"
holesky = "${HOLESKY_RPC_URL}"
fuji = "${FUJI_RPC_URL}"
anvil = "${ANVIL_RPC_URL}"
avalanche = "${AVALANCHE_RPC_URL}"

# Etherscan API Key for contract verification
[etherscan]
mainnet = { key = "${ETHERSCAN_API_KEY}" }
sepolia = { key = "${ETHERSCAN_API_KEY}" }
holesky = { key = "${ETHERSCAN_API_KEY}" }
fuji = { key = "${SNOWTRACE_FUJI_API_KEY}", url = "https://api-testnet.snowtrace.io/api" }
avalanche = { key = "${SNOWTRACE_API_KEY}", url = "https://api.snowtrace.io/api" }

# See more config options https://github.com/foundry-rs/foundry/blob/master/crates/config/README.md#all-options
